git -v - Retorna a versão do git (útil para ver se o computador tem git instalado)
git init - Inicia o git dentro do repositório adicionando a pasta oculta “.git”
git diff - Mostra as diferenças do conteúdo do arquivo atual em comparação com o último commit
